#!/usr/bin/env ruby

begin
	require 'sploder'
rescue LoadError
	require 'rubygems'
	require 'sploder'
end

# This line is what prints out "Upload class loaded" when installing
#require 'sploder/upload'
input = ARGV[0]
puts "Main Sploder executable running with #{input}"

if ARGV[0] == "upload"
	puts "Upload function"
end

